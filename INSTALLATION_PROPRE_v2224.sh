#!/bin/bash

# ═══════════════════════════════════════════════════════════════════════════════
# 🚀 INSTALLATION PROPRE VERSION 2224
# ═══════════════════════════════════════════════════════════════════════════════
#
# Ce script remplace COMPLÈTEMENT votre version actuelle par v2224
# ATTENTION : Toutes vos modifications seront perdues !
#
# ═══════════════════════════════════════════════════════════════════════════════

set -e  # Arrêter en cas d'erreur

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🚀 INSTALLATION PROPRE - VERSION 2224"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "⚠️  ATTENTION : Ce script va REMPLACER complètement votre version actuelle !"
echo ""
read -p "Êtes-vous sûr de vouloir continuer ? (oui/non) : " confirmation

if [ "$confirmation" != "oui" ]; then
  echo "❌ Installation annulée."
  exit 1
fi

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📋 ÉTAPES À SUIVRE MANUELLEMENT"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "✅ ÉTAPE 1 : Télécharger v2224 depuis Figma Make"
echo "   1. Dans Figma Make, cliquer sur l'icône téléchargement"
echo "   2. Télécharger le fichier ZIP"
echo "   3. Le sauvegarder quelque part (ex: ~/Downloads/smartcabb.zip)"
echo ""
read -p "Avez-vous téléchargé le ZIP ? (oui/non) : " downloaded

if [ "$downloaded" != "oui" ]; then
  echo "❌ Veuillez d'abord télécharger le ZIP depuis Figma Make."
  exit 1
fi

echo ""
read -p "Entrez le chemin complet du fichier ZIP téléchargé : " ZIP_PATH

if [ ! -f "$ZIP_PATH" ]; then
  echo "❌ Fichier non trouvé : $ZIP_PATH"
  exit 1
fi

echo ""
echo "✅ Fichier ZIP trouvé : $ZIP_PATH"
echo ""

# ═══════════════════════════════════════════════════════════════════════════════
# ÉTAPE 2 : BACKUP DE SÉCURITÉ
# ═══════════════════════════════════════════════════════════════════════════════

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📦 ÉTAPE 2 : Backup de sécurité"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

CURRENT_DIR=$(pwd)
PARENT_DIR=$(dirname "$CURRENT_DIR")
BACKUP_DIR="$PARENT_DIR/smartcabb-backup-$(date +%Y%m%d-%H%M%S)"

echo "Création du backup dans : $BACKUP_DIR"
cp -r "$CURRENT_DIR" "$BACKUP_DIR"
echo "✅ Backup créé avec succès"
echo ""

# ═══════════════════════════════════════════════════════════════════════════════
# ÉTAPE 3 : SAUVEGARDER LE DOSSIER .git
# ═══════════════════════════════════════════════════════════════════════════════

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📂 ÉTAPE 3 : Sauvegarder le dossier .git"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

GIT_BACKUP="/tmp/smartcabb-git-backup-$(date +%Y%m%d-%H%M%S)"
cp -r "$CURRENT_DIR/.git" "$GIT_BACKUP"
echo "✅ Dossier .git sauvegardé dans : $GIT_BACKUP"
echo ""

# ═══════════════════════════════════════════════════════════════════════════════
# ÉTAPE 4 : SUPPRIMER TOUS LES FICHIERS (SAUF .git)
# ═══════════════════════════════════════════════════════════════════════════════

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🗑️  ÉTAPE 4 : Supprimer les anciens fichiers"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# Supprimer tous les fichiers SAUF .git
find . -mindepth 1 -maxdepth 1 ! -name '.git' -exec rm -rf {} +
echo "✅ Anciens fichiers supprimés"
echo ""

# ═══════════════════════════════════════════════════════════════════════════════
# ÉTAPE 5 : EXTRAIRE LE ZIP
# ═══════════════════════════════════════════════════════════════════════════════

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📦 ÉTAPE 5 : Extraction de v2224"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# Extraire dans un dossier temporaire
TEMP_EXTRACT="/tmp/smartcabb-extract-$(date +%Y%m%d-%H%M%S)"
mkdir -p "$TEMP_EXTRACT"
unzip -q "$ZIP_PATH" -d "$TEMP_EXTRACT"

# Trouver le dossier racine (au cas où le ZIP a un dossier parent)
EXTRACT_ROOT=$(find "$TEMP_EXTRACT" -name "package.json" -exec dirname {} \; | head -n 1)

if [ -z "$EXTRACT_ROOT" ]; then
  echo "❌ Erreur : package.json non trouvé dans le ZIP"
  exit 1
fi

echo "✅ Fichiers extraits de : $EXTRACT_ROOT"
echo ""

# Copier tous les fichiers
cp -r "$EXTRACT_ROOT/"* "$CURRENT_DIR/"
cp -r "$EXTRACT_ROOT/".* "$CURRENT_DIR/" 2>/dev/null || true

echo "✅ Fichiers v2224 copiés"
echo ""

# Nettoyer
rm -rf "$TEMP_EXTRACT"

# ═══════════════════════════════════════════════════════════════════════════════
# ÉTAPE 6 : RESTAURER LE DOSSIER .git
# ═══════════════════════════════════════════════════════════════════════════════

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📂 ÉTAPE 6 : Restaurer le dossier .git"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

rm -rf "$CURRENT_DIR/.git"
cp -r "$GIT_BACKUP" "$CURRENT_DIR/.git"
echo "✅ Dossier .git restauré"
echo ""

# ═══════════════════════════════════════════════════════════════════════════════
# ÉTAPE 7 : VÉRIFIER GIT
# ═══════════════════════════════════════════════════════════════════════════════

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🔍 ÉTAPE 7 : Vérification Git"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

git status
echo ""

# ═══════════════════════════════════════════════════════════════════════════════
# ÉTAPE 8 : COMMIT
# ═══════════════════════════════════════════════════════════════════════════════

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "💾 ÉTAPE 8 : Commit des changements"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

git add -A

git commit -m "🚀 Version 2224 - Installation propre complète

- Version stable v517.36
- Tous les fichiers remplacés
- Configuration production optimisée
- Prêt pour déploiement Vercel

Installation automatique via script"

echo "✅ Commit créé"
echo ""

# ═══════════════════════════════════════════════════════════════════════════════
# ÉTAPE 9 : DEMANDER CONFIRMATION POUR LE PUSH
# ═══════════════════════════════════════════════════════════════════════════════

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🚀 ÉTAPE 9 : Push vers GitHub"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "⚠️  Voulez-vous pusher maintenant vers GitHub ?"
echo "   Cela va remplacer TOUT sur GitHub et déclencher un déploiement Vercel."
echo ""
read -p "Pusher maintenant ? (oui/non) : " push_now

if [ "$push_now" == "oui" ]; then
  echo ""
  echo "Push vers GitHub..."
  git push origin main || git push -f origin main
  
  echo ""
  echo "✅ Push réussi !"
  echo ""
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo "🎉 INSTALLATION TERMINÉE AVEC SUCCÈS !"
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""
  echo "📋 Prochaines étapes :"
  echo ""
  echo "1. Attendre le déploiement Vercel (3-5 minutes)"
  echo "   → https://vercel.com/dashboard"
  echo ""
  echo "2. Tester l'application"
  echo "   → https://smartcabb.com"
  echo ""
  echo "3. Vérifier les 3 interfaces :"
  echo "   ✅ Passager"
  echo "   ✅ Conducteur"
  echo "   ✅ Admin"
  echo ""
  echo "📂 Backup de votre ancienne version :"
  echo "   → $BACKUP_DIR"
  echo ""
else
  echo ""
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo "✅ INSTALLATION LOCALE TERMINÉE"
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""
  echo "Les fichiers sont prêts mais PAS ENCORE pushés vers GitHub."
  echo ""
  echo "Pour pusher manuellement plus tard :"
  echo "  git push origin main"
  echo ""
  echo "Ou forcer le push si refusé :"
  echo "  git push -f origin main"
  echo ""
  echo "📂 Backup de votre ancienne version :"
  echo "   → $BACKUP_DIR"
  echo ""
fi

# Nettoyer le backup .git temporaire
rm -rf "$GIT_BACKUP"

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✨ Script terminé"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
